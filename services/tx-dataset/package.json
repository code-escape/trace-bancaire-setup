{
  "name": "tx-dataset",
  "version": "1.0.0",
  "description": "Trace bancaire — dataset AML calibré déterministe + API paginée (épreuve 3)",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=20"
  }
}
